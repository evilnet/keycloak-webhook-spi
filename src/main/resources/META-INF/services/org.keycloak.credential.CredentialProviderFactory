# Disabled - ScramCredentialProvider interferes with X3's pre-hashed credential imports
# X3 generates SCRAM credentials itself when it has plaintext
# This provider should only be used for passwords set through Keycloak UI
# TODO: Fix provider to not interfere with Admin API credential imports
